# Per-user activity log format (http-context; installed to /etc/nginx/conf.d).
# The reaper uses studio-activity.log recency to decide when a user's session
# services can be stopped.
log_format studio_activity '$time_iso8601 $studio_user $status "$request"';
